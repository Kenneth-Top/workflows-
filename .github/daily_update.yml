name: Daily Data Scrape

on:
  schedule:
    # 每天 UTC 时间 00:00 运行 (北京时间早上 8:00)
    - cron: '0 0 * * *'
  workflow_dispatch: # 允许手动点击按钮触发

jobs:
  scrape-and-update:
    runs-on: ubuntu-latest
    
    steps:
      - name: Check out repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install pandas openpyxl playwright plotly streamlit
          playwright install chromium

      - name: Run Scraper Script
        run: python web_openrouter_tokens_for_all_models.py  # <--- 把这里改成你爬虫脚本的文件名

      - name: Commit and Push if changes
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git add latest_summary.csv
          git commit -m "🚀 Auto-update data [skip ci]" || echo "No changes to commit"
          git push
